---
type: Term
title: Scope
---
Who a rule applies to: `universal` (craft, every project), `personal` (Fabio's taste, own brands and unbranded work), `client:<slug>` (a client constraint, never a default), `project:<slug>` (one project only). Scope is what keeps a client palette out of the portfolio.
