---
type: Term
title: Promote / retire / rescope
---
The three review verdicts. Promote moves a candidate to `decisions/` with a fresh `DB-###`; retire keeps it in `inbox/` marked `retired` so a harvester never resurfaces it; rescope changes who it applies to.
