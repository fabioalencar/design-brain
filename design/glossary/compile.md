---
type: Term
title: Compile
---
`bun run compile` renders `skills/` and `exports/` from confirmed decisions and patterns. `--preview` includes unreviewed candidates and stamps every line and file as such. Skills are build artifacts; editing one by hand is a smell.
