---
type: Term
title: Pattern
---
A named, quotable thing a reviewer can match on a screen, in `patterns/`. `kind: anti-slop` is a recurring failure to detect; `kind: craft` is something that should be present. Patterns have no promotion step; they are edited or deleted.
