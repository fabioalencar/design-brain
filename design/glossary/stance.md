---
type: Term
title: Stance
---
How hard a rule binds: `always`, `never`, `prefer`, `avoid`, `context`. Always/never become non-negotiables in `design-brain-start`; prefer/avoid are defaults an agent may deviate from with a stated reason.
